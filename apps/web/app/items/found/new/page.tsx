import { NewItemPage } from '@/components/items/NewItemPage';

export const metadata = { title: 'Lapor Barang Temuan — AMTEHI' };

export default function NewFoundItemPage() {
  return <NewItemPage type="found" />;
}
